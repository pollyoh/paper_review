# 논문 분석 보고서 작성 프롬프트

아래 프롬프트를 복사하여, `{논문 경로}`만 교체하면 동일한 품질의 보고서를 생성할 수 있다.

---

## 프롬프트

```
/paper 에서 {논문 경로} 를 읽고 한국어로 분석하는 보고서를 작성해줘.

### 작성 원칙

1. **방법론 디테일**: 논문에서 사용한 방법을 구체적으로, 수식과 알고리즘 수준까지 설명할 것
2. **시각 자료**: 표(table)와 mermaid 다이어그램을 적극 활용하여 이해를 도울 것
3. **주석(annotation)**: 이해가 어려운 수학적/기술적 개념에는 별도 주석 블록으로 직관적 해설을 추가할 것. 주석 제목에는 해당 주석이 다루는 내용을 짧게 요약하여 표기한다 (예: `> **[주석] 어댑터(Adapter)란?**`, `> **[주석] 음의 로그우도 최소화로 변환하는 이유**`)
4. **정리된 형식**: 목차, 섹션 구분, TL;DR 요약을 포함한 구조화된 보고서로 작성할 것
5. **논문 원본 이미지 활용**: 논문 경로에 PDF와 함께 PNG 파일(Figure 1.png, Figure 2.png 등)이 존재하면, 이는 원논문에서 추출한 그림이다. 텍스트나 mermaid만으로는 전달이 어렵고 **해당 이미지가 설명에 실질적으로 도움이 되는 경우에만** 보고서에 포함한다. 모든 이미지를 무조건 나열하지 말 것.
   - 포함 기준: 아키텍처 다이어그램, 실험 결과 그래프, 시스템 구조도 등 시각적 이해가 필수인 경우
   - 제외 기준: 텍스트/표로 충분히 대체 가능하거나, 해상도가 낮아 가독성이 떨어지는 경우
   - 삽입 형식: `![Figure N 설명](../paper/{논문폴더명}/Figure N.png)` (report/ 기준 상대경로)

### 웹서치 및 스토리 조사 (필수)

반드시 해당 논문과 관련한 웹서치를 수행하여 아래 내용을 조사하고 보고서에 포함할 것:

- **사건/영향**: 이 논문의 기법이 실제 현장(학회, 산업, 정책 등)에서 사용되거나 영향을 미친 사례
- **기술적 계보**: 이 논문이 작성되기까지 어떤 선행 연구/기술적 발전이 있었는지
- **학계 반응**: 지지 의견, 비판, 논쟁이 있다면 양측 입장을 균형 있게 정리
- **배경 지식**: 스토리를 이해하기 위해 부가적으로 필요한 개념도 자세히 안내

시간이 오래 걸려도 좋으니 가장 질 좋은 보고서를 작성하도록 일해.

### 보고서 저장 위치

- 디렉토리: `/report/`
- 파일명: `{YYYYMMDD}_report_{논문명_snake_case}.md`
  - 예시: `20250409_report_detecting_llm_generated_peer_reviews.md`
- frontmatter 없이 순수 마크다운으로 작성한다
- 웹 배포(`src/content/reviews/` 배치, frontmatter 추가, 이미지 경로 변환)는 별도의 "Astro 빌드 및 배포 프롬프트"에서 처리한다

### 보고서 구조

아래 7개 섹션을 반드시 포함할 것. 논문 내용에 따라 세부 항목은 유연하게 조정 가능.

#### 1. 배경 및 문제 정의
- 보고서 맨 위에 논문 메타정보를 표기하되, 각 항목(원논문 제목, 저자, 소속, 출처, 보고서 작성일)은 `<br>`로 줄바꿈 처리하여 한 줄씩 표시한다. 예시:
  ```
  **원논문**: {영문 제목}<br>
  **저자**: {저자 목록}<br>
  **소속**: {소속 기관}<br>
  **출처**: {학회/저널/arXiv 등}<br>
  **보고서 작성일**: {YYYY-MM-DD}
  ```
- 해당 분야의 현황과 문제의식
- 기존 접근법의 한계

#### 2. 핵심 방법론 상세 설명
- 논문이 제안하는 방법의 각 구성요소를 개별 하위 섹션으로 분리
- 수식이 있으면 LaTeX로 표기하고, 주석 블록으로 직관적 의미를 해설
- 알고리즘이 있으면 의사코드 + 설명을 병행
- 핵심 아이디어를 mermaid 다이어그램으로 시각화
- 방법론 간 비교가 가능하면 표로 정리

#### 3. 실험 결과 요약
- 주요 실험 설정 (데이터셋, 모델, 평가 지표)
- 핵심 결과를 한국어 표로 재구성 (원논문 표를 번역/재정리)
- Ablation study, 방어/공격 실험 등 부가 실험이 있으면 포함
- 결과에 대한 해석과 시사점

#### 4. 관련 스토리 및 실제 영향
- 웹서치를 통해 확인한 사건, 사례, 보도 내용
- 타임라인이 있으면 mermaid 다이어그램으로 정리
- 커뮤니티 반응 (지지/비판 양측)
- 관련 정책 변화나 후속 조치

#### 5. 기술적 배경 지식
- 논문을 이해하기 위해 필요한 선행 지식
- 핵심 알고리즘/이론의 원리 설명
- 관련 연구 흐름 (이 논문의 기술적 계보)
- 기존 도구/방법과의 비교

#### 6. 논문의 한계 및 향후 전망
- 저자가 언급한 한계점
- 보고서 작성자가 판단한 추가 한계점
- 윤리적 고려사항 (해당 시)
- 향후 연구 방향

#### 7. 참고문헌 및 관련 자료
- 원논문 및 코드 저장소 링크
- 관련 뉴스/블로그 포스트 링크
- 배경 연구 논문 목록
- 학회/기관 정책 문서 링크

### 스타일 가이드라인

- 한국어 작성, 전문 용어는 영문 병기 (예: 가족별 오류율(FWER))
- 표와 mermaid 다이어그램을 적극 활용
- 수학 수식은 LaTeX 형식 ($$ ... $$)으로 표기
- 각 섹션 시작 시 핵심 요약(TL;DR)을 blockquote로 포함
- 이해가 어려운 개념에는 > **[주석]** 블록으로 상세 해설 추가
- 코드/알고리즘은 코드 블록으로 표기
- 이모지 사용 금지
```

---

## 사용 예시

```
/paper 에서 @paper/my_new_paper.pdf 를 읽고 한국어로 분석하는 보고서를 작성해줘.
... (위 프롬프트 전문 붙여넣기)
```

## 사전 준비

1. 분석할 논문 PDF를 `/paper/` 디렉토리에 넣는다
2. 위 프롬프트의 `{논문 경로}`를 실제 파일 경로로 교체한다
3. 프롬프트를 전달하면 AI가 논문 읽기, 웹서치, 보고서 작성을 순차적으로 수행한다
4. 보고서 완성 후, "Astro 빌드 및 배포 프롬프트"를 이어서 실행하면 웹 배포까지 완료된다

## 보고서 품질 체크리스트

보고서가 완성되면 아래 항목을 확인한다:

- 7개 섹션이 모두 포함되었는가
- 보고서 상단 메타정보가 각 항목별로 `<br>` 줄바꿈 처리되었는가
- 주석 제목에 해당 주석의 내용 요약이 포함되었는가 (예: `[주석] 어댑터란?`)
- 논문의 핵심 방법론이 수식/알고리즘 수준으로 설명되었는가
- 표가 3개 이상 포함되었는가
- mermaid 다이어그램이 2개 이상 포함되었는가
- 주석 블록으로 어려운 개념이 보충 설명되었는가
- 웹서치 기반 관련 스토리가 포함되었는가
- 참고문헌에 URL 링크가 포함되었는가
- 파일명이 `{YYYYMMDD}_report_{논문명}.md` 형식인가 (`/report/`)

---

## Astro 빌드 및 배포 프롬프트

마크다운 보고서가 완성된 후, 아래 프롬프트로 Astro 빌드 및 배포를 수행한다.

### 프롬프트

```
방금 작성한 보고서를 Astro 사이트에 배포해줘.

### 프로젝트 구조

이 프로젝트는 Astro SSG로 빌드되어 GitHub Pages에 배포된다.
마크다운 파일을 `src/content/reviews/`에 배치하면 Astro가 자동으로 HTML을 생성한다.
마크다운 frontmatter가 메타데이터 역할을 한다.

paper_study/
  astro.config.mjs
  src/
    content.config.ts              # Content Collection 스키마
    content/reviews/*.md           # 보고서 마크다운 (frontmatter 포함)
    components/PaperSearch.tsx     # 메인 페이지 검색/필터 컴포넌트
    layouts/ReviewLayout.astro     # 리뷰 페이지 레이아웃
    pages/index.astro              # 메인 페이지
    pages/reviews/[...slug].astro  # 동적 리뷰 라우트
    styles/                        # CSS (수정 금지)
    scripts/                       # 런타임 JS (수정 금지)
  public/images/reviews/{slug}/    # 논문 원본 이미지 (사용된 것만 복사)
  paper/                           # 논문 PDF + 원본 이미지 (소스)
  report/                          # 보고서 원본
  dist/                            # 빌드 출력 (CI가 배포)

### 수행할 작업 (순서대로)

#### 1단계: src/content/reviews/ 에 마크다운 생성

`/report/`의 원본 보고서를 기반으로, frontmatter를 추가하여 `src/content/reviews/{YYYYMMDD}-{논문명-kebab-case}.md`를 생성한다.

frontmatter 스키마 (`src/content.config.ts` 기준):

```yaml
---
title: "{한국어 보고서 제목}"
originalTitle: "{원논문 영문 제목}"
date: YYYY-MM-DD
authors: "{저자 목록}"
institution: "{소속 기관}"
tags: ["{태그1}", "{태그2}"]
description: "{1~2문장 한국어 요약}"
---
```
```

frontmatter 아래에 보고서 본문을 그대로 포함한다.

#### 1-1단계: 이미지 처리 (해당 시)

보고서에 논문 원본 이미지(PNG)가 포함된 경우:

1. `paper/{논문폴더}/`에서 사용된 이미지만 `public/images/reviews/{slug}/`로 복사한다
2. 마크다운 내 이미지 경로를 `/images/reviews/{slug}/Figure N.png` 형식으로 수정한다

#### 2단계: Astro 빌드

`npm run build`를 실행한다. 빌드 오류가 발생하면 오류를 분석하고 수정한다.
빌드가 성공하면 `dist/` 에 정적 사이트가 생성된다.

#### 3단계: 렌더링 확인

`npm run preview`를 실행하고 브라우저로 다음을 확인한다:

1. 메인 페이지에서 새 카드가 정상 표시되는지 확인
2. 카드를 클릭하여 리뷰 페이지로 이동이 되는지 확인
3. 리뷰 페이지에서 다음이 정상 렌더링되는지 확인:
  - 표(table)
  - mermaid 다이어그램
  - KaTeX 수식
  - 주석 토글 (접이식)
  - TOC 사이드바
  - "목록으로" 네비게이션

#### 4단계: 배포 확인

렌더링이 정상이면 사용자에게 배포 여부를 물어본다:
"빌드 및 렌더링이 정상입니다. git commit 후 push하면 GitHub Actions가 자동으로 배포합니다. push 할까요?"

### 주의사항

- `docs/` 폴더는 레거시이므로 수정하지 않는다
- HTML을 수작업으로 생성하지 않는다 (Astro가 빌드 시 자동 생성)
- `src/styles/`, `src/scripts/`, `src/layouts/`, `src/components/`는 수정하지 않는다
- `src/content.config.ts`의 스키마를 수정하지 않는다

```

### 사용 예시

```

방금 작성한 보고서를 Astro 사이트에 배포해줘.
... (위 프롬프트 전문 붙여넣기)

```

### 배포 체크리스트

- [ ] `src/content/reviews/{slug}.md` 생성됨 (frontmatter 포함)
- [ ] frontmatter 필드가 스키마와 일치하는가 (title, originalTitle, date, authors, institution, tags, description)
- [ ] 사용된 논문 이미지가 `public/images/reviews/{slug}/`에 복사됨 (이미지 미사용 시 해당 없음)
- [ ] `npm run build` 성공
- [ ] 메인 페이지에서 새 카드 표시 정상
- [ ] 리뷰 페이지 이동 정상
- [ ] 표, mermaid, KaTeX, 주석 토글, TOC 렌더링 정상
- [ ] 사용자에게 배포(push) 여부 확인 완료
```

